{% extends 'back.html.twig' %}

{% block title %}Participation{% endblock %}

{% block body %}
    <h1>Participation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>id</th>
                <td>{{ participation.id }}</td>
            </tr>
            <tr>
                <th>IdUser</th>
                <td>{{ participation.User }}</td>
            </tr>
            <tr>
                <th>DatePart</th>
                <td>{{ participation.datePart ? participation.datePart|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_participation_index') }}">back to list</a>

    <a href="{{ path('app_participation_edit', {'id': participation.id}) }}">edit</a>

    {{ include('participation/_delete_form.html.twig') }}
{% endblock %}
