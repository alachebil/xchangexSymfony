{% extends 'frontL.html.twig' %}

{% block title %}New Livraison{% endblock %}

{% block body %}

    <a href="{{ path('app_livraison_home') }}"class="btn btn-info rounded-pill m-2" ><i class="fa fa-arrow-left" ></i></a>

    <h1>Create new Livraison</h1>
 {{ form_start(form,{'attr': {'novalidate': 'novalidate'}} ) }}

    {{ include('livraison/_form.html.twig') }}
{% endblock %}
